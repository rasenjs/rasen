{
  "name": "rasen-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "yarn build:client && yarn build:server",
    "build:client": "vite build --ssrManifest --outDir dist/client",
    "build:server": "vite build --ssr src/entry-server.tsx --outDir dist/server",
    "preview": "vite preview"
  },
  "dependencies": {
    "@rasenjs/core": "0.1.5-alpha",
    "@rasenjs/html": "0.1.3-alpha",
    "@rasenjs/jsx-runtime": "0.1.6-alpha",
    "@rasenjs/reactive-signals": "0.1.4-alpha",
    "@rasenjs/router": "0.1.7-alpha",
    "@rasenjs/web": "0.1.3-alpha",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
