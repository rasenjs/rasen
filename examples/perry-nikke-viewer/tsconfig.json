{
  "//": [
    "Typecheck-only config for the Perry NIKKE viewer.",
    "",
    "Perry erases types, so it will compile nonsense that tsc rejects. This",
    "config catches misuse of the rasen / WebGPU / perry-ui API surfaces before",
    "paying for a native build: `yarn tsc -p .`",
    "",
    "`perry/ui` is not an npm package - the compiler injects it and the runtime",
    "ships no declarations. The vendored copy under `types/perry/` comes from the",
    "Perry source tree (`types/perry/ui/index.d.ts`) so this example typechecks",
    "without a checkout present.",
    "",
    "`jsx: preserve` + `jsxImportSource` are for TSX files under src/. Perry does",
    "its own JSX lowering (it emits `jsx(type, props)` calls and routes them to",
    "whatever binding the module imports - see src/perry-ui/jsx-runtime.ts), so",
    "`preserve` keeps tsc from rewriting the syntax it needs to see."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["node"],
    "jsx": "preserve",
    "jsxImportSource": "@local/jsx",
    "baseUrl": ".",
    "paths": {
      "perry/ui": ["types/perry/ui/index.d.ts"],
      "perry/thread": ["types/perry/thread/index.d.ts"],
      "@local/jsx/jsx-runtime": ["src/perry-ui/jsx-runtime.ts"],
      "@local/jsx/jsx-dev-runtime": ["src/perry-ui/jsx-runtime.ts"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "types/**/*.d.ts"]
}
